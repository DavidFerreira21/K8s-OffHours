apiVersion: v1
kind: Namespace
metadata:
  name: app-dev-deploy-scope
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout-api
  namespace: app-dev-deploy-scope
  labels:
    offhours.platform.io/schedule: "dev"
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: checkout-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: checkout-api
    spec:
      containers:
        - name: api
          image: nginx:1.27
          ports:
            - containerPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: billing-worker
  namespace: app-dev-deploy-scope
  labels:
    app.kubernetes.io/name: billing-worker
    offhours.platform.io/schedule: "dev"
  annotations:
    offhours.platform.io/protected: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: billing-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: billing-worker
    spec:
      containers:
        - name: worker
          image: busybox:1.36
          command: ["sh", "-c", "sleep 3600"]
